"use strict";

class Task {
  constractor(description) {
    this.id = Math.floor(Math.random() * 101 );
    this.description = this.description;
    this.complete = false;
  }
}

class TaskManager {}
